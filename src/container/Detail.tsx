import React, { useState, useEffect } from 'react';
import AnimeDetailLink from '~/component/AnimeDetailLink';
const AnimeList = (): JSX.Element => {
	return (
		<div>
			<AnimeDetailLink></AnimeDetailLink>
		</div>
	);
};

export default AnimeList;
